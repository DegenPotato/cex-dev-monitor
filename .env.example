# Server Configuration
PORT=3001
NODE_ENV=development

# Super Admin Wallets (comma-separated Solana addresses)
# Only these wallets can access the dashboard
# Example: wallet1,wallet2,wallet3
VITE_SUPER_ADMIN_WALLETS=

# JWT Authentication (REQUIRED for auth system)
# Generate secure key with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Secret Access Code (for VR/Testing - grants super_admin)
# Default: SNIFFAGENCY
# Change this in production!
SECRET_ACCESS_CODE=SNIFFAGENCY

# Database
DB_PATH=./monitor.db

# CORS (comma-separated origins - add your Vercel domains)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,https://cex-dev-monitor.vercel.app,https://alpha.sniff.agency,https://sniff.agency

# Proxies (optional)
PROXY_FILE=./proxies.txt

# CEX Wallet (configure in UI)
# CEX_WALLET=your_wallet_address_here

# YouTube Music Integration (optional)
# Setup Guide:
# 1. Go to: https://console.cloud.google.com/
# 2. Create a new project or select existing
# 3. Enable YouTube Data API v3:
#    - Go to APIs & Services → Enable APIs
#    - Search for "YouTube Data API v3" and enable it
# 4. Create API Key:
#    - Go to APIs & Services → Credentials
#    - Click "Create Credentials" → API Key
#    - Copy the key to VITE_YOUTUBE_API_KEY below
# 5. Create OAuth 2.0 Client ID:
#    - Click "Create Credentials" → OAuth client ID
#    - Application type: Web application
#    - Add authorized JavaScript origins:
#      - http://localhost:5173
#      - http://localhost:3000
#      - Your production domain (e.g., https://sniff.agency)
#    - Add authorized redirect URIs (same as origins)
#    - Copy the Client ID to VITE_YOUTUBE_CLIENT_ID below
VITE_YOUTUBE_API_KEY=your_youtube_api_key_here
VITE_YOUTUBE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
