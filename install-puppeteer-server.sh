#!/bin/bash

# SSH into server and install puppeteer with dependencies
ssh -i "C:\Users\Potato\.ssh\id_ed25519_new" root@139.59.237.215 << 'EOF'
cd /var/www/cex-monitor

# Install Chrome dependencies for puppeteer
apt-get update
apt-get install -y \
    ca-certificates \
    fonts-liberation \
    libappindicator3-1 \
    libasound2 \
    libatk-bridge2.0-0 \
    libatk1.0-0 \
    libc6 \
    libcairo2 \
    libcups2 \
    libdbus-1-3 \
    libexpat1 \
    libfontconfig1 \
    libgbm1 \
    libgcc1 \
    libglib2.0-0 \
    libgtk-3-0 \
    libnspr4 \
    libnss3 \
    libpango-1.0-0 \
    libpangocairo-1.0-0 \
    libstdc++6 \
    libx11-6 \
    libx11-xcb1 \
    libxcb1 \
    libxcomposite1 \
    libxcursor1 \
    libxdamage1 \
    libxext6 \
    libxfixes3 \
    libxi6 \
    libxrandr2 \
    libxrender1 \
    libxss1 \
    libxtst6 \
    lsb-release \
    wget \
    xdg-utils

# Pull latest code
git pull

# Install puppeteer
npm install puppeteer

# Rebuild backend
npm run build:backend

# Restart PM2
pm2 restart cex-monitor

echo "✅ Puppeteer installed and backend rebuilt!"
EOF
